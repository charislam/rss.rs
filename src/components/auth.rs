use leptos::prelude::*;

pub(crate) fn AuthPage() -> impl IntoView {
    view! {
        <div>
            <h1>"Authentication Page"</h1>
            <p>"This is the authentication page."</p>
        </div>
    }
}